version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    volumes:
      - ./data/db:/data/db
    command: mongod

  backend:
    depends_on:
      - mongo
    build:
      context: .
      dockerfile: backend_dockerfile
    restart: always
    ports:
      - "80:80"